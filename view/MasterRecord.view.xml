<mvc:View
        controllerName="ilim.pdm2.vacation_planning.controller.MasterRecord"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m"
        xmlns:l="sap.ui.layout"
        displayBlock="true"
        height="100%">

    <FlexBox class="sapUiLargeMarginTop"
             alignItems="Center"
             justifyContent="Center">
        <items>
            <Table fixedLayout="false" class="sapUiTinyMarginTop" items="{data>/}">
                <headerToolbar>
                    <Toolbar>
                        <ToolbarSpacer/>
                        <Button	type="Emphasized" text="Создать период" press="onPeriodCreate"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column vAlign="Middle" hAlign="Center">
                        <Text text="{i18n>masterRecord.table.year}" />
                    </Column>
                    <Column vAlign="Middle" hAlign="Center">
                        <Text text="{i18n>masterRecord.table.bukrs}" />
                    </Column>
                    <Column vAlign="Middle" hAlign="Center">
                        <Text text="{i18n>masterRecord.table.status}" />
                    </Column>
                    <Column hAlign="Center">
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{data>year}"/>
                            <Text text="{data>bukrs_text}"/>
                            <ObjectStatus text="{path: 'data>status', formatter: '.formatter.periodStatus'}"
                                          state="{path: 'data>status', formatter: '.formatter.periodState'}"/>
                            <Button icon="{path: 'data>status', formatter: '.formatter.periodActionIcon'}"
                                    text="{path: 'data>status', formatter: '.formatter.periodAction'}"
                                    type="{path: 'data>status', formatter: '.formatter.periodActionType'}"
                                    enabled="{path: 'data>status', formatter: '.formatter.periodActionEnabled'}"
                                    press="onUpdateMasterRecord"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </items>
    </FlexBox>

</mvc:View>