<mvc:View
        controllerName="ilim.pdm2.vacation_planning.controller.ApprovalManage"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m"
        xmlns:f="sap.f"
        displayBlock="true"
        height="100%">

    <Table fixedLayout="false" class="sapUiMediumMarginTop" mode="MultiSelect" items="{inbox>/}"
           selectionChange="onEmployeeSelect" rememberSelections="true"
           id="inboxTable">
        <headerToolbar>
            <OverflowToolbar>
                <Title text="{i18n>main.toolbar.title}" level="H2"/>
                <SearchField width="30%" placeholder="{i18n>main.toolbar.searchPlaceholder}" liveChange="onEmployeeSearch"/>
                <ToolbarSpacer/>
                <OverflowToolbarButton type="Transparent" icon="sap-icon://multiselect-all"/>
                <OverflowToolbarButton type="Transparent" icon="sap-icon://filter"/>
                <OverflowToolbarButton type="Transparent" icon="sap-icon://excel-attachment"/>
                <OverflowToolbarButton type="Transparent" icon="sap-icon://action-settings"/>
                <Button icon="sap-icon://display" text="{i18n>main.toolbar.showSelected}" press="onShowSelected"/>
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column vAlign="Middle" hAlign="Begin">
                <Text text="{i18n>main.table.employee}" />
            </Column>
            <Column vAlign="Middle" hAlign="Center">
                <Text text="{i18n>main.table.department}" />
            </Column>
            <Column vAlign="Middle" hAlign="Center">
                <Text text="{i18n>main.table.employeeId}" />
            </Column>
            <Column vAlign="Middle" hAlign="Center">
                <Text text="{i18n>main.table.lineManager}" />
            </Column>
            <Column vAlign="Middle" hAlign="Center">
                <Text text="{i18n>main.table.daysStats}" />
            </Column>
            <Column vAlign="Middle" hAlign="Center">
                <Text text="{i18n>main.table.hasAccess}" />
            </Column>
            <Column vAlign="Middle" hAlign="Center">
                <Text text="{i18n>main.table.status}" />
            </Column>
            <Column hAlign="Center">
            </Column>
        </columns>
        <items>
            <ColumnListItem class="sapUiTinyPaddingTop" highlight="{inbox>highlight}">
                <cells>
                    <FlexBox alignItems="Center">
                        <items>
                            <f:Avatar displaySize="XS" class="sapUiTinyMargin" src="{inbox>picture/medium}"/>
                            <ObjectIdentifier
                                    title="{inbox>name}"
                                    text="{inbox>position}"/>
                        </items>
                    </FlexBox>
                    <VBox>
                        <Label text="{inbox>department}" />
                        <Label text="{inbox>location}" />
                    </VBox>
                    <ObjectIdentifier
                            text="{inbox>employeeId}"/>
                    <Text   textAlign="Center"
                            text="{inbox>lineManager}"/>
                    <ObjectNumber number="{inbox>daysBalance}/{inbox>availableDays}/{inbox>plannedDays}"
                                  unit="{i18n>main.table.units}"/>
                    <CheckBox enabled="false" selected="{inbox>hasAccess}" />
                    <ObjectStatus
                            text="{inbox>planStatus}"
                            state="Success" />
                    <FlexBox alignItems="Center" justifyContent="SpaceBetween">
                        <items>
                            <Button type="Accept" icon="sap-icon://accept" enabled="false"/>
                            <Button type="Reject" icon="sap-icon://decline" enabled="false"/>
                            <Button icon="sap-icon://create-form" enabled="false"/>
                            <Button icon="sap-icon://comment" enabled="false"/>
                        </items>
                    </FlexBox>

                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>